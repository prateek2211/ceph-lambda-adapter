# Ceph Lambda Adapter
Simple Adapter that invokes AWS lambda on recieving ceph bucket notification

## Prerequisites
* CMake
* AWS SDK for C++

## Build Instructions
```shell script
$ mkdir "build"
$ cd build
$ cmake ../
$ make && make install
```